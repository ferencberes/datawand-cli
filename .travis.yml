language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
# command to install dependencies
install:
  - pip install .
# command to run tests
script:
  - pytest
  - bash -e datawandcli/tests/cli_test.sh
